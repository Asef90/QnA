= link_to '+', send("vote_#{resource.class.name.downcase}_path", resource, value: 1),
               method: :post,
               remote: true,
               data: { type: :json },
               class: 'vote'

= link_to '-', send("vote_#{resource.class.name.downcase}_path", resource, value: -1),
               method: :post,
               remote: true,
               data: { type: :json },
               class: 'vote'
